import pages from './pages';
import other from './other';
import applicationLayer from "./application-layer";
import technologyLayer from "./technology-layer";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [
    applicationLayer,
    technologyLayer,
    pages,
    other
  ]
};

export default menuItems;
