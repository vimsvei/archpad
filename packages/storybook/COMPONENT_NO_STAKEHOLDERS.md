# 👥 Component Detail - No Stakeholders

Специальный вариант карточки компонента архитектуры для случаев, когда стейкхолдеры не заданы.

---

## 🎯 Назначение

Этот компонент создан для того, чтобы явно показать пользователям, что для архитектурного компонента не назначены стейкхолдеры (ответственные лица, заинтересованные стороны), и предоставить удобный способ их добавления.

---

## 📍 Как использовать

### 1. Из страницы списка компонентов

На странице `/components`:

1. Нажмите на любой компонент в списке
2. В верхнем меню выберите версию **"No Stakeholders"**
3. Откроется карточка компонента с пустым состоянием для стейкхолдеров

### 2. Программно

```tsx
import { ComponentDetailNoStakeholders } from '@/app/components/component-detail-no-stakeholders';

<ComponentDetailNoStakeholders
  component={myComponent}
  onBack={() => console.log('Back to list')}
  onSave={(component) => console.log('Saved:', component)}
/>
```

---

## 🎨 Особенности дизайна

### 1. **Alert предупреждение** (верх страницы)

```
⚠️ Для этого компонента не назначены стейкхолдеры. 
   Рекомендуется добавить ответственных лиц.
   [Добавить →]
```

- Желтый/amber цвет для привлечения внимания
- Иконка `AlertCircle` от lucide-react
- CTA кнопка "Добавить" для быстрого действия

### 2. **Empty State** (правая панель)

```
┌─────────────────────────┐
│    👤 (UserPlus icon)   │
│                         │
│  Стейкхолдеры не заданы │
│  Добавьте ответственных │
│  лиц и заинтересованные │
│  стороны                │
│                         │
│  [+ Добавить стейкхолдера] │
└─────────────────────────┘
```

- Dashed border для визуального выделения
- Центрированный контент
- Иконка `UserPlus` в круге
- Текстовое пояснение
- Призыв к действию (CTA) кнопка

### 3. **Badge** (в header)

```
[No Stakeholders]
```

- Outline variant для subtle appearance
- Отображается рядом с другими badges (state, etc.)

---

## 🔧 Компоненты используемые

### UI Components
- `Alert` + `AlertDescription` - предупреждение вверху
- `Button` - CTA кнопки для добавления стейкхолдеров
- `Badge` - индикатор "No Stakeholders"
- `Tooltip` - подсказки для кнопок

### Icons (lucide-react)
- `AlertCircle` - иконка предупреждения в alert
- `UserPlus` - иконка добавления стейкхолдера
- `ArrowLeft` - кнопка назад
- `Save` - кнопка сохранения
- `Layers` - иконка компонента

---

## 📐 Layout структура

```
┌─────────────────────────────────────────────────────────────┐
│ Header: [← Back]  [Icon] Component Name        [Badge] [Save]│
├─────────────────────────────────────────────────────────────┤
│ ⚠️ Alert: No stakeholders assigned [Add Button]            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────┬─────────────────────┐          │
│  │ Description (2/3)     │ Properties (1/3)    │          │
│  │                       │                     │          │
│  │ [Textarea]            │ Code:               │          │
│  │                       │ State:              │          │
│  │                       │ License:            │          │
│  │                       │ Architecture:       │          │
│  ├───────────────────────┤ Critical Level:     │          │
│  │ Relations             │                     │          │
│  │ (Horizontal Scroll)   ├─────────────────────┤          │
│  │ [Business][App][Tech] │ Stakeholders        │          │
│  │                       │ ┌─────────────────┐ │          │
│  ├───────────────────────┤ │  Empty State    │ │          │
│  │ Flows Table           │ │  [+ Add]        │ │          │
│  │                       │ └─────────────────┘ │          │
│  └───────────────────────┴─────────────────────┘          │
│                                                             │
│  ┌───────────────────────────────────────────┐            │
│  │ Diagram (Full Width)                      │            │
│  └───────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎬 User Flow

### Сценарий 1: Добавление стейкхолдера из Alert

1. Пользователь видит Alert предупреждение вверху
2. Нажимает кнопку "Добавить" в Alert
3. **TODO:** Открывается Sheet/Dialog для выбора стейкхолдера
4. Пользователь выбирает человека и роль
5. Стейкхолдер добавлен в компонент

### Сценарий 2: Добавление из Empty State

1. Пользователь скроллит до раздела "Стейкхолдеры" справа
2. Видит Empty State с пояснением
3. Нажимает кнопку "Добавить стейкхолдера"
4. **TODO:** Открывается Sheet/Dialog для выбора
5. Стейкхолдер добавлен

### Сценарий 3: Игнорирование предупреждения

1. Пользователь видит Alert, но не добавляет стейкхолдера
2. Может редактировать другие поля компонента
3. Сохраняет изменения
4. Alert остается видимым при следующем просмотре

---

## 💻 Код - ключевые части

### Alert блок

```tsx
<Alert className="mb-6 border-amber-500/50 bg-amber-500/10">
  <AlertCircle className="size-4 text-amber-500" />
  <AlertDescription className="text-sm">
    <div className="flex items-center justify-between">
      <span className="text-foreground">
        Для этого компонента не назначены стейкхолдеры. 
        Рекомендуется добавить ответственных лиц.
      </span>
      <Button 
        variant="outline" 
        size="sm" 
        onClick={handleAddStakeholder}
        className="ml-4 shrink-0"
      >
        <UserPlus className="size-4 mr-2" />
        Добавить
      </Button>
    </div>
  </AlertDescription>
</Alert>
```

### Empty State блок

```tsx
<div className="border-2 border-dashed border-border/50 rounded-lg p-6 text-center bg-muted/10">
  <div className="flex flex-col items-center gap-3">
    <div className="size-12 rounded-full bg-muted/50 grid place-items-center">
      <UserPlus className="size-6 text-muted-foreground" />
    </div>
    <div>
      <p className="text-sm font-medium text-foreground mb-1">
        Стейкхолдеры не заданы
      </p>
      <p className="text-xs text-muted-foreground">
        Добавьте ответственных лиц и заинтересованные стороны
      </p>
    </div>
    <Button 
      variant="outline" 
      size="sm" 
      onClick={handleAddStakeholder}
      className="mt-2"
    >
      <UserPlus className="size-4 mr-2" />
      Добавить стейкхолдера
    </Button>
  </div>
</div>
```

---

## 🔄 TODO для интеграции

### 1. Добавление стейкхолдера

```tsx
const handleAddStakeholder = () => {
  toast.info('Добавление стейкхолдера - функция в разработке');
  // TODO: Open stakeholder selection sheet
  // Реализовать:
  // - Открыть Sheet справа
  // - Показать список доступных людей
  // - Выбор роли (Owner, Contributor, Viewer, etc.)
  // - API call: useAddStakeholderMutation()
};
```

### 2. API Integration

Заменить mock данные на RTK Query:

```tsx
// Вместо useState использовать:
const { data: stakeholders, isLoading } = useGetComponentStakeholdersQuery(component.id);

// Для добавления:
const [addStakeholder] = useAddStakeholderMutation();

const handleAddStakeholder = async (stakeholderId: string, role: string) => {
  try {
    await addStakeholder({ 
      componentId: component.id, 
      stakeholderId, 
      role 
    }).unwrap();
    toast.success('Стейкхолдер добавлен');
  } catch (error) {
    toast.error('Ошибка при добавлении стейкхолдера');
  }
};
```

### 3. i18n перевод

Добавить переводы для всех текстов:

```tsx
import { useTranslate } from '@tolgee/react';

const { t } = useTranslate();

// Использование:
<AlertDescription>
  {t('component.no_stakeholders.warning')}
</AlertDescription>
```

---

## 🎨 Storybook

Запустите Storybook чтобы увидеть все варианты:

```bash
pnpm storybook
```

Доступные stories:
- **Basic Component** - компонент с минимальными данными
- **Extended Relations** - компонент с множеством связей
- **Minimal Data** - компонент почти без данных
- **With Flows** - компонент с входящими/исходящими потоками

Все stories показывают empty state для стейкхолдеров.

---

## 🎯 Use Cases

### Когда использовать этот компонент?

1. **Новые компоненты** - только что созданные, еще не распределены
2. **Legacy компоненты** - старые системы без назначенных владельцев
3. **Аудит** - выявление компонентов без ответственных
4. **Onboarding** - обучение новых пользователей важности стейкхолдеров

### Когда НЕ использовать?

- Если стейкхолдеры уже назначены (используйте ComponentDetailV3)
- Если стейкхолдеры опциональны для данного типа компонента

---

## 📊 Метрики

Потенциальные метрики для отслеживания:

- Процент компонентов без стейкхолдеров
- Время до назначения первого стейкхолдера
- Клики на кнопку "Добавить стейкхолдера"
- Conversion rate: просмотр → добавление стейкхолдера

---

## ♿ Accessibility

- ✅ Alert имеет `role="alert"` для screen readers
- ✅ Кнопки имеют descriptive labels
- ✅ Tooltips для дополнительного контекста
- ✅ Keyboard navigation работает во всех элементах
- ✅ Color contrast соответствует WCAG AA

---

## 🔍 Связанные компоненты

- `ComponentDetailV3` - минималистичная версия (с стейкхолдерами)
- `ComponentDetailV2` - сгруппированная версия
- `ComponentDetail` - оригинальная версия
- `DirectoryItemDetail` - аналог для справочников

---

## 📝 Примечания

1. **Цвет Alert**: Amber/Yellow используется для предупреждения, но не для ошибки
2. **Empty State**: Дизайн можно настроить под брендинг
3. **CTA размещение**: Две CTA кнопки (в Alert и в Empty State) для удобства
4. **Badge**: Помогает быстро идентифицировать карточку в селекторе версий

---

**Создано для ArchPad Enterprise Architecture Tool** 🚀
