---
id: "cta"
titleKey: "landing.cta.title"
bodyKey: "landing.cta.body"
primaryKey: "site.cta.requestDemo"
primaryHref: "https://portal.archpad.pro"
secondaryKey: "site.cta.openDocs"
secondaryHref: "https://portal.archpad.pro"
---

<Section id="cta">
  <CTA
    title={<T keyName={frontmatter.titleKey} />}
    body={<T keyName={frontmatter.bodyKey} />}
    primary={frontmatter.primaryKey && frontmatter.primaryHref ? { labelKey: frontmatter.primaryKey, href: frontmatter.primaryHref } : undefined}
    secondary={frontmatter.secondaryKey && frontmatter.secondaryHref ? { labelKey: frontmatter.secondaryKey, href: frontmatter.secondaryHref } : undefined}
  />
</Section>
