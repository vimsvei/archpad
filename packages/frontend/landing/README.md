# ArchPad Landing Page

Современный лендинг для ArchPad — архитектурного репозитория и системы портфолио менеджмента. Проект использует React, TypeScript, Tailwind CSS v4 и Velite CMS для управления контентом через Markdown файлы.

## 🚀 Технологии

- **React 18** — UI библиотека
- **TypeScript** — статическая типизация
- **Vite** — сборщик и dev-сервер
- **React Router** (`react-router`) — маршрутизация
- **Tailwind CSS v4** — utility-first CSS (без config файла)
- **Velite** — CMS на базе Markdown файлов
- **shadcn/ui** — компонентная библиотека
- **Motion** (`motion/react`) — библиотека анимаций
- **Lucide React** — иконки
- **React Hook Form** — работа с формами

## 🎨 Дизайн-система

### Цветовая палитра

Основной корпоративный цвет из логотипа ArchPad:
- **Primary**: `#7CB342` (lime-green)
- **Primary Dark**: `#689F38` (hover состояния)
- **Primary Light**: `#9CCC65` (акценты)
- **Primary Very Light**: `#C5E1A5` (фоны, декоративные элементы)
- **Background Light**: `#F1F8E9` (светлые секции)

### Применение в коде

```jsx
// Прямые HEX значения
className="bg-[#7CB342] text-white"
className="hover:bg-[#689F38]"
className="border-[#7CB342]"

// Градиенты
className="bg-gradient-to-br from-[#7CB342] via-[#689F38] to-[#558B2F]"
```

### Принципы дизайна

1. Современный, чистый интерфейс
2. Плавные анимации при скролле (Motion)
3. Адаптивная верстка (mobile-first)
4. Иллюстрации в стиле Storyset
5. Закругленные кнопки (`rounded-full`)
6. Градиенты для hero-секций

## 📁 Структура проекта

```
/
├── content/               # Markdown контент для Velite CMS
│   ├── benefits/         # Преимущества продукта (4 файла)
│   ├── features/         # Функции продукта (6 файлов)
│   ├── use-cases/        # Кейсы использования (6 файлов)
│   ├── faq/              # Часто задаваемые вопросы (6 файлов)
│   ├── legal/            # Юридические документы (3 файла)
│   └── text-blocks/      # Текстовые блоки компонентов (13 файлов)
├── public/
│   └── images/           # SVG иллюстрации (Storyset стиль)
├── src/
│   ├── app/
│   │   ├── components/   # React компоненты
│   │   │   ├── ui/      # shadcn/ui компоненты (60+)
│   │   │   └── figma/   # Системные компоненты
│   │   ├── pages/       # Страницы приложения (15 страниц)
│   │   ├── lib/         # Утилиты и хелперы
│   │   ├── hooks/       # React хуки
│   │   ├── routes.tsx   # Конфигурация роутинга
│   │   └── App.tsx      # Точка входа
│   ├── styles/          # CSS файлы
│   │   ├── theme.css    # Дизайн-токены
│   │   ├── fonts.css    # Импорты шрифтов
│   │   ├── tailwind.css # Tailwind v4 конфигурация
│   │   └── index.css    # Главный CSS
│   └── content.d.ts     # TypeScript типы для Velite
├── velite.config.ts     # Конфигурация Velite CMS
├── .velite/             # Сгенерированные данные Velite
├── vite.config.ts       # Конфигурация Vite
├── AGENTS.md            # Инструкции для AI агентов
└── README.md            # Этот файл
```

## 🎯 Управление контентом через Velite CMS

Весь контент лендинга управляется через Markdown файлы в папке `content/`. Это позволяет легко редактировать тексты, добавлять новые разделы и поддерживать актуальность информации без изменения кода компонентов.

### Коллекции контента

#### 1. Features (функции продукта)

Создайте файл в `content/features/`:

```markdown
---
title: "Название функции"
description: "Описание функции"
icon: "FolderTree"
color: "from-blue-500 to-cyan-500"
order: 1
---
```

Доступные иконки: [Lucide Icons](https://lucide.dev/icons/)

#### 2. Benefits (преимущества)

Создайте файл в `content/benefits/`:

```markdown
---
stat: "85%"
label: "Описание преимущества"
order: 1
---
```

#### 3. Use Cases (кейсы использования)

Создайте файл в `content/use-cases/`:

```markdown
---
title: "Enterprise Architecture"
description: "Описание кейса использования"
icon: "Building2"
image: "/images/usecase-enterprise-architecture.svg"
tags:
  - "Enterprise"
  - "Architecture"
  - "Governance"
order: 1
---
```

#### 4. FAQ (часто задаваемые вопросы)

Создайте файл в `content/faq/`:

```markdown
---
question: "Вопрос?"
answer: "Подробный ответ на вопрос."
category: "general"
order: 1
---
```

#### 5. Legal Documents (юридические документы)

Создайте файл в `content/legal/`:

```markdown
---
slug: "privacy-policy"
title: "Политика конфиденциальности"
subtitle: "Как мы защищаем ваши данные"
icon: "Shield"
lastUpdated: "12 февраля 2026"
content: |
  # Основной контент документа
  
  Текст политики конфиденциальности в формате Markdown...
---
```

#### 6. Text Blocks (текстовые блоки)

Используются для хранения контента компонентов (Hero, Features, Footer и т.д.):

```markdown
---
slug: "hero"
title: "Hero Section"
metadata:
  headline: "Заголовок главной секции"
  description: "Описание продукта"
  primaryCTA: "Начать бесплатно"
  secondaryCTA: "Запросить демо"
---
```

### Использование контента в коде

```tsx
import { features, benefits, useCases, faq } from '#content';

export function Features() {
  return (
    <div>
      {features.map((feature) => (
        <div key={feature.title}>
          <h3>{feature.title}</h3>
          <p>{feature.description}</p>
        </div>
      ))}
    </div>
  );
}
```

## 🛠️ Команды

```bash
# Установка зависимостей
npm install

# Сборка контента из Markdown файлов
npm run velite

# Очистка кэша Velite и пересборка
npm run velite:clean

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build
```

## 🔗 Основные маршруты

- `/` — Главная страница
- `/pricing` — Тарифные планы
- `/register` — Регистрация пользователей
- `/contact` — Контактная форма
- `/design-partner-program` ��� Программа для дизайн-партнеров
- `/privacy-policy` — Политика конфиденциальности
- `/terms-of-service` — Условия использования
- `/cookie-policy` — Политика Cookie
- `/features/*` — Детальные страницы функций

## 💰 Ценообразование

### Тарифные планы SaaS

| План | Цена (₽/мес) | Цена ($/мес) |
|---------|--------------|--------------|
| Starter | 3,490₽ | $39 |
| Team | 8,990₽ | $99 |
| Business | 17,990₽ | $199 |
| Enterprise | Свяжитесь с отделом продаж | Contact Sales |

**Курс конвертации**: 90₽ = 1$

### On-Premise решения

Отдельные решения с установкой на серверах клиента. Цена по запросу.

## 📝 Компоненты

### Основные секции лендинга

1. **Header** — шапка с навигацией
2. **Hero** — главный баннер с CTA
3. **Features** — функции продукта (6 карточек)
4. **Benefits** — преимущества и статистика (4 метрики)
5. **HowItWorks** — как это работает (3 шага)
6. **UseCases** — кейсы использования (6 сценариев)
7. **Integrations** — интеграции с другими системами
8. **FAQ** — часто задаваемые вопросы (аккордеон)
9. **CTA** — призыв к действию
10. **EarlyAccess** — ранний доступ
11. **Footer** — футер с ссылками

### Страницы

- **Home** — главная страница (компонует все секции)
- **Pricing** — тарифные планы с переключателем валют
- **Register** — форма регистрации пользователей
- **Contact** — контактная форма (с поддержкой query параметров)
- **DesignPartnerProgram** — программа для партнеров
- **Legal Pages** — политики и условия использования
- **Feature Detail Pages** — детальные страницы функций
- **NotFound** — 404 страница

## 🎨 Анимации

Используется библиотека Motion для плавных анимаций:

```tsx
import { motion } from 'motion/react';

<motion.div
  initial={{ opacity: 0, y: 20 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true }}
  transition={{ duration: 0.6 }}
>
  Анимированный контент
</motion.div>
```

## 📋 Формы

### Контактная форма

Поддерживает предустановку темы через query параметр:
- `/contact?subject=demo` — запрос демонстрации

Темы обращения:
- Запрос демонстрации
- Техническая поддержка
- Коммерческие вопросы
- Партнерство
- Другое

### Форма регистрации

Поля:
- Полное имя
- Email
- Компания
- Пароль
- Подтверждение пароля
- Согласие с политикой конфиденциальности (обязательно)

## 🔒 Важные правила

### ❌ Не делать

- НЕ использовать `react-router-dom` (только `react-router`)
- НЕ создавать `tailwind.config.js` (используется Tailwind v4)
- НЕ импортировать Motion как `framer-motion`
- НЕ редактировать контент напрямую в компонентах (использовать Markdown)

### ✅ Делать

- Использовать `react-router` для навигации
- Следовать дизайн-системе (#7CB342)
- Редактировать контент через Markdown файлы
- Добавлять анимации Motion для улучшения UX
- Писать адаптивный код (mobile-first)
- Использовать TypeScript с типизацией

## 🐛 Отладка

### Velite ошибки

```bash
# Очистить кэш и пересобрать
npm run velite:clean
```

### Контент не обновляется

```bash
# Пересобрать контент
npm run velite
```

### YAML parse errors

Убедитесь, что все значения в frontmatter заключены в кавычки, если содержат спецсимволы:

```yaml
✗ title: Централизованный репозиторий: все в одном месте
✓ title: "Централизованный репозиторий: все в одном месте"
```

## 📚 Дополнительная информация

Для детальных инструкций по работе с проектом и AI агентами смотрите файл `AGENTS.md`.

## 🎯 Целевая аудитория

- Enterprise архитекторы
- Solution архитекторы
- CTO и технические руководители
- Product менеджеры
- Команды разработки

## 📞 Контакты

Проект разрабатывается для **ArchPad** — платформы управления архитектурой и портфолио.

---

**Текущая версия**: 1.0.0  
**Последнее обновление**: 12 февраля 2026  
**Статус**: ✅ Production Ready
