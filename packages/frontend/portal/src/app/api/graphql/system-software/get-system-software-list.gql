query GetSystemSoftwareList($where: SystemSoftware_bool_exp!, $limit: Int!, $offset: Int!) {
  SystemSoftware(where: $where, order_by: { name: asc }, limit: $limit, offset: $offset) {
    id
    code
    name
  }
  SystemSoftwareAggregate(where: $where) {
    aggregate {
      count
    }
  }
}

