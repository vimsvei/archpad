/**
 * Shared technology radar area.
 *
 * NOTE: Values are Tolgee keys; keep them stable.
 */
export enum TechnologyRadarZone {
  ADOPT = "radar.zone.adopt",
  TRIAL = "radar.zone.trial",
  ACCESS = "radar.zone.access",
  HOLD = "radar.zone.hold",
}

