/**
 * Единые параметры отображения примитивов схемы.
 * Здесь держим всё, что влияет на внешний вид (радиусы, размеры, отступы),
 * чтобы не разносить магические числа по компонентам.
 */

export const SCHEMA_STYLE = {
  /** Минимальная высота вложенных элементов (иконка 24px + паддинги) */
  itemMinHeightPx: 30,

  /** Радиусы скругления */
  radius: {
    /** Прямоугольники (interfaces, data objects, technology blocks) */
    rectPx: 0,
    /** “Пилюли” (functions, events) */
    pillPx: 10,
  },

  /** Размеры хендлов у вложенных элементов */
  nestedHandle: {
    sizePx: 6,
    offsetPx: 4,
    opacity: {
      idle: 0.18,
      hover: 0.55,
    },
  },

  /** Горизонтальный gap между элементами в строке */
  rowGapPx: 5,
} as const


