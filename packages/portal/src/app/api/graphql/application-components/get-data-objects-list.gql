query GetDataObjectsList($where: data_objects_bool_exp!, $limit: Int!, $offset: Int!) {
  data_objects(where: $where, order_by: { name: asc }, limit: $limit, offset: $offset) {
    id
    code
    name
  }
  data_objects_aggregate(where: $where) {
    aggregate {
      count
    }
  }
}

