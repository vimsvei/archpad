export enum MotivationPriority {
  MUST = 'must', // критическое, обязательно
  SHOULD = 'should', // желательно, важно
  COULD = 'could', // хорошо бы, если успеем/есть ресурсы
  WONT = 'wont', // явно зафиксированное "не делаем сейчас"
}
