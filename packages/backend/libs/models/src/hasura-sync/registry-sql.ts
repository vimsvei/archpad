/**
 * @deprecated
 * `hasura_sync.*` registry is now defined as MikroORM entities and created via SchemaGenerator.
 *
 * The handwritten SQL used to live here; we keep the file as a “pointer” for future readers.
 */
export const HASURA_SYNC_REGISTRY_SQL = '';
