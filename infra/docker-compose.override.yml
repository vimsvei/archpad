services:
  # Expose Postgres to localhost (for psql / DBeaver / TablePlus)
  postgres:
    ports:
      - "${PG_PORT}:5432"

  # Expose MinIO S3 API to localhost (для локального Strapi)
  minio:
    ports:
      - "127.0.0.1:9000:9000"    # S3 API (use http://127.0.0.1:9000)
