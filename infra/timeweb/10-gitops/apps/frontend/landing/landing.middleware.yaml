apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: landing-api-lead-ratelimit
  namespace: platform
  annotations:
    argocd.argoproj.io/sync-wave: "72"
spec:
  rateLimit:
    average: 10
    burst: 20
    sourceCriterion:
      ipStrategy:
        depth: 1
