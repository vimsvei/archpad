apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: landing
  namespace: platform
  annotations:
    argocd.argoproj.io/sync-wave: "75"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`archpad.pro`)
      kind: Rule
      services:
        - name: landing
          port: 3000
  tls:
    secretName: wildcard-archpad-pro-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: landing-http
  namespace: platform
  annotations:
    argocd.argoproj.io/sync-wave: "75"
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`archpad.pro`)
      kind: Rule
      services:
        - name: landing
          port: 3000
      middlewares:
        - name: redirect-to-https
          namespace: traefik
