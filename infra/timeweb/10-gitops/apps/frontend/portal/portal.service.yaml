apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: platform
  annotations:
    argocd.argoproj.io/sync-wave: "65"
  labels:
    app: portal
spec:
  selector:
    app: portal
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
