apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: platform
  annotations:
    argocd.argoproj.io/sync-wave: "40"
  labels:
    app: pgadmin
spec:
  type: ClusterIP
  selector:
    app: pgadmin
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
