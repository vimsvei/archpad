apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "40"
spec:
  redirectScheme:
    scheme: https
    permanent: true
