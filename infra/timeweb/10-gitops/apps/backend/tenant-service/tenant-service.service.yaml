apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: platform
  annotations:
    argocd.argoproj.io/sync-wave: "65"
  labels:
    app: tenant-service
spec:
  selector:
    app: tenant-service
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
